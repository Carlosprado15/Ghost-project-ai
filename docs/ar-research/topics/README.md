# topics/

Um arquivo por pergunta respondida, nomeado `AR-KB-XXX.md` (XXX = número
sequencial com 3 dígitos, ex. `AR-KB-001.md`), seguindo exatamente o
template obrigatório definido em `../INDEX.md`. Máximo 120 linhas por
arquivo — se o assunto precisar de mais espaço, dividir em `AR-KB-XXX` e
`AR-KB-YYY` separados, nunca estourar o limite num arquivo só.

Vazio até o primeiro ciclo de pesquisa rodar.
